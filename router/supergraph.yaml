subgraphs:
  subgraph:
    routing_url: http://subgraph:4001/
    schema:
      sdl: |
        type Query {
          hello: String
          user: User
        }

        type User {
          id: ID
          friends: [User]
        }
